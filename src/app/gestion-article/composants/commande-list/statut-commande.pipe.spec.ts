import { StatutCommandePipe } from './statut-commande.pipe';

describe('StatutCommandePipe', () => {
  it('create an instance', () => {
    const pipe = new StatutCommandePipe();
    expect(pipe).toBeTruthy();
  });
});
